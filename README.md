A basic Check in system for CamJam raspberry pi jam
to make finding & confirming tickets eassier
than the previous printed sheets
Designed to run on a raspberry pi (for Obvious reasons)
using Python3 & Flask to provide an interactive web browser interface.
simply export the Ticket list to a CSV file & run the setup script setup.sh
then run with python CamJam.py